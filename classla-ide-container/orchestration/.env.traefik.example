# Traefik Configuration
# Copy this file to .env and update with your values

# Domain for IDE containers (required)
# All containers will be accessible at subdomains of this domain
# Example: container123-code.ide.example.com
DOMAIN=ide.example.com

# Email for Let's Encrypt certificate notifications (required)
# You'll receive expiration warnings and important updates
ACME_EMAIL=admin@example.com

# Traefik Dashboard Authentication (optional but recommended)
# Generate with: htpasswd -nb admin yourpassword
# Default: admin / admin (CHANGE THIS!)
TRAEFIK_DASHBOARD_AUTH=admin:$apr1$8EVjn/nj$GiLUZqcbueTFeD23SuB6x0

# Notes:
# - Wildcard DNS (*.yourdomain.com) must point to your server IP
# - Ports 80 and 443 must be open in your firewall
# - Docker Swarm must be initialized before deploying Traefik
